/**
 * Sets the document title to the given string, plus the site name.
 * @param {string} title The title to set.
 * @returns {void}
 */
function setPageTitle(title)
{
    document.title = `${title} || WatermelonKatana`;
}